load("@gazelle//:def.bzl", "gazelle")

# gazelle:prefix github.com/mocoarow/cocotola-1.24
# gazelle:go_test_separate_targets small,medium,large

gazelle(name = "gazelle")

gazelle(
    name = "gazelle-update-repos",
    args = [
        "-from_file=go.work",
        "-to_macro=deps.bzl%go_dependencies",
        "-prune",
    ],
    command = "update-repos",
)
